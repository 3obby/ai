const { PrismaClient } = require("@prisma/client"); const prisma = new PrismaClient(); async function main() { const companions = await prisma.companion.findMany({ select: { id: true, name: true, userName: true, description: true, src: true }}); console.log(JSON.stringify(companions, null, 2)); } main().then(async () => { await prisma.$disconnect(); }).catch(async (e) => { console.error(e); await prisma.$disconnect(); process.exit(1); });
